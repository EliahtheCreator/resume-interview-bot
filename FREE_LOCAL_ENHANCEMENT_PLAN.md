# 完全免费本地增强路线图

目标：尽量不调用付费 API，把产品做到可用、可信、可持续迭代。所有增强优先使用浏览器原生能力、本地规则和开源前端库。

## 已完成

- PDF 文本解析：`pdfjs-dist`
- 扫描版 PDF OCR：浏览器渲染 PDF 页面后用 `Tesseract.js`
- 语音输入：浏览器 Web Speech API
- 基于简历片段出题：本地规则提取岗位关键词、项目片段、数字指标
- 岗位差异化题库：AI 产品经理、视频生成算法、招聘 HR
- 本地追问：按回答长度、个人贡献、指标、取舍、岗位关键词触发
- 本地评分报告：按权重输出总分、亮点、风险、建议、面试官疑虑
- PWA 外壳：支持安装和缓存应用资源
- 报告导出：导出 Markdown 报告

## 优先级 P0：马上最有用

1. 转写确认页
   - 语音识别后先展示转写文本，让用户确认再提交。
   - 仍然可以保持“回答必须用语音”，但允许修正识别错误。

2. 单题即时反馈
   - 每道题回答后给 2-3 条规则化反馈。
   - 例如：缺指标、缺个人贡献、缺取舍、偏离岗位关键词。

3. 简历解析可信度
   - 显示解析方式：文本直读 / OCR。
   - 显示文本长度、识别页数、是否识别到指标、是否识别到岗位关键词。

4. 岗位题库配置文件化
   - 把现在写在 `server.mjs` 里的 rubric 移到 JSON。
   - 方便后续手工维护，不需要改代码。

## 优先级 P1：让练习更像产品

1. 弱项专项练习
   - 报告后根据最低维度生成 3 道专项练习题。
   - 不需要模型，只用本地题库和规则。

2. 历史趋势
   - 展示最近几次分数变化。
   - 展示最常见弱项：缺指标、缺贡献、缺取舍、表达过短等。

3. 报告打印 / PDF
   - 浏览器原生打印即可，不需要付费服务。

4. 本地数据管理
   - 删除单次报告。
   - 清空全部历史。
   - 导出/导入本地 JSON 备份。

## 优先级 P2：隐私和上线体验

1. 隐私说明页
   - 明确不保存原始录音。
   - 明确保存邮箱、简历文本、转写文本、报告。

2. 本地加密
   - 使用 Web Crypto API 对本地历史做浏览器端加密。
   - 用户自己设置口令。

3. 离线使用提示
   - PWA 已缓存应用外壳。
   - OCR 和语音识别仍依赖浏览器能力，离线可用性取决于浏览器。

4. 白名单管理
   - 简单管理允许使用的邮箱。
   - 保持不收费，但避免陌生人乱用。

## 免费方案的硬边界

- 不能像大模型一样真正理解复杂语义。
- 不能准确判断高深技术回答是否正确。
- 浏览器语音识别兼容性不完全稳定。
- OCR 对模糊、倾斜、小字、复杂版式简历效果有限。
- 本地规则评分适合练习反馈，不适合作为正式招聘评估依据。

